diff --git a/src/widget-label.ts b/src/widget-label.ts
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/widget-label.ts
@@ -0,0 +1,9 @@
+const label = widget!.label
+const first = items.at(0)!
+const item = rows[0]!;
+call(value!, other)
+if (a != b) return
+if (a !== b) return
+if (!ready) return
+const shout = 'hi!'
+const ok = !isEmpty(list)
