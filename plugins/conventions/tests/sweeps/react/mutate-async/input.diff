diff --git a/src/use-submit-order.ts b/src/use-submit-order.ts
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/use-submit-order.ts
@@ -0,0 +1,3 @@
+const result = await someMutation.mutateAsync(variables)
+someMutation.mutate(variables, { onSuccess })
+const { mutateAsync: submitOrder } = useSubmitOrder()
