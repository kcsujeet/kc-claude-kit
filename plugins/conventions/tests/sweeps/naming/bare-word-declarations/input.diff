diff --git a/src/use-retention-policy.ts b/src/use-retention-policy.ts
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/use-retention-policy.ts
@@ -0,0 +1,9 @@
+const check = <A extends unknown[], R>(action: (...args: A) => R) => action
+let guard = false
+export const checkRetentionPolicy = () => true
+function run() {}
+const RETRY_DELAY_MS = 3000
+const Widget = () => null
+return { value }
+// a constant is declared below
+const [first, second] = pair
